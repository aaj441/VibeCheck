[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd houses-app/backend && npm install"]

[phases.build]
cmds = ["cd houses-app/backend && npm run build"]

[start]
cmd = "cd houses-app/backend && npm start"
